__pycache__/
*.pyc
.pytest_cache/
.venv/
venv/
*.egg-info/
build/
dist/
# ephemeral connector HTTP cache (fixtures are the pinned, committed real data)
keystone/connectors/cache/
# generated demo artifacts (reproducible via run_workbench.py / build.py)
demo_out/
browser_out/
.DS_Store
# local secrets — API keys live in .env and are NEVER committed
.env
.env.*
!.env.example
# design-tool exports (Figma/handoff bundles — not source code)
*.zip
# internal strategy note — working doc, not part of the public deliverable
docs/top3-strategy.md
